<Application x:Class="WpfAppTRPO_reader.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfAppTRPO_reader"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Century Gothic"/>
            <Setter Property="Foreground" Value="#9D5D91"/>
        </Style>
        <Style TargetType="Control">
            <Setter Property="FontFamily" Value="Century Gothic"/>
        </Style>
        <Style TargetType="DataGrid">
            <!-- Общие параметры таблицы -->
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontFamily" Value="Century Gothic"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="#9D5D91"/>
            <Setter Property="BorderBrush" Value="#9D5D91"/>
            <Setter Property="BorderThickness" Value="1"/>
            <!-- Линии сетки между ячейками (границы) -->
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#9D5D91"/>
            <Setter Property="VerticalGridLinesBrush" Value="#9D5D91"/>
            <!-- Стиль для обычных ячеек -->
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <!-- Выравнивание содержимого по центру -->
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <!-- Наследуем цвет шрифта и фон -->
                        <Setter Property="Foreground" Value="#9D5D91"/>
                        <Setter Property="Background" Value="White"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <!-- Стиль для заголовков столбцов -->
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="#9D5D91"/>
                        <Setter Property="BorderBrush" Value="#9D5D91"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Century Gothic"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#9D5D91"/>
            <Setter Property="Background" Value="#FCC165"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </Application.Resources>
</Application>
