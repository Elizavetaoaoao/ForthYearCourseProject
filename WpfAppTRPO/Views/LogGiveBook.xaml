<Window x:Class="WpfAppTRPO.Views.LogGiveBook"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppTRPO.Views"
        mc:Ignorable="d"
        Background="#D37EAB"
        Title="Выдача книги" Height="240" Width="400" WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="#9D5D91"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#FCC165"/>
            <Setter Property="Margin" Value="20,0,20,20"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#9D5D91"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>
    <StackPanel Orientation="Vertical" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Читатель:" VerticalAlignment="Center" Margin="5"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Книга:" VerticalAlignment="Center" Margin="5"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Экземпляр" VerticalAlignment="Center" Margin="5"/>
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Вернуть до:" VerticalAlignment="Center" Margin="5"/>

            <ComboBox Grid.Row="0" Grid.Column="1" Name="comboBoxReaders" Margin="5" Height="30" DisplayMemberPath="ФИО" SelectedValuePath="ID"/>
            <ComboBox Grid.Row="1" Grid.Column="1" Name="comboBoxBooks" Margin="5" Height="30" DisplayMemberPath="Title" SelectedValuePath="ID" SelectionChanged="comboBoxBookCopies_SelectionChanged"/>
            <ComboBox Grid.Row="2" Grid.Column="1" Name="comboBoxBookCopies" Margin="5" Height="30" DisplayMemberPath="ID" SelectedValuePath="ID"/>
            <TextBlock x:Name="textBlockDueDate" Grid.Row="3" Grid.Column="1" Text="01.01.2026" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5"/>
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Name="buttonGive" Content="Выдать" Width="100" Height="35" Margin="10" Click="ButtonGive_Click"/>
            <Button Name="buttonCancel" Content="Отмена" Width="100" Height="35" Margin="10" Click="ButtonCancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
